apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: ingressroute
spec:
  entrypoints:
  - web
  routes:
  - match: Host(`api.traefik.com`)
    kind: Rule
    services:
    - name: nginx-service
      port: 80